import { IState } from './models/state.model';
export declare const generateBlocks: (sizeX: number, sizeY: number, fillWith?: number) => number[];
export declare const getRandomInt: (min: number, max: number) => number;
export declare const getTestingState: (blocks: boolean[], sizeX: number, sizeY: number, index: number, rotation: number, x: number, y: number) => IState;
