import { IState } from './models/state.model';
import { SimpleStore } from './simple-redux';
export declare type Store = SimpleStore<IState>;
export interface ICreateStoreOptions {
    matrixSizeX: number;
    matrixSizeY: number;
}
export declare type StoreFactory = (options?: ICreateStoreOptions) => Store;
export declare const createStore: StoreFactory;
