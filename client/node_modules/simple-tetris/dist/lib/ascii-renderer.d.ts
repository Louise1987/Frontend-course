import * as fromGame from './game';
export interface IAsciiFrame {
    data: string;
    width: number;
    height: number;
}
export declare const render: (state: fromGame.IState) => IAsciiFrame;
